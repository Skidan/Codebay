let com = compare(x);
let num = 1;
let lower = 8, upper = 12;

function compare(num) {
  return function(y) {
    if (i < num) {console.log("The number "+num+" is bigger than "+i);}
    elseif (i = num) {console.log("theese numbers are equal");}
    elseif {out("The number "+num+" is smaller then "+i);}
  };
}

for (i=lower; i<=upper; i++){
  console.log(com(num));
}